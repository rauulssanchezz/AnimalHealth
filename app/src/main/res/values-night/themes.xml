<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AnimalHealth" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryNight</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkNight</item>
        <item name="colorAccent">@color/colorAccentNight</item>
        <item name="android:windowBackground">@color/colorBackgroundNight</item>

        <!-- Establecimiento de colores para botones, BottomNavigationView y CardView -->
        <item name="colorButtonNormal">@color/colorButtonNight</item>
        <item name="colorControlNormal">@color/colorButtonNight</item> <!-- Para widgets que usan el tema AppCompat -->
        <item name="colorControlActivated">@color/colorButtonNight</item>
        <item name="colorControlHighlight">@color/colorButtonNight</item>
        <item name="cardBackgroundColor">@color/colorCardViewNight</item>

        <!-- Personalización del texto de los botones -->
        <item name="colorOnSurface">@color/colorButtonTextNight</item>

        <!-- Personalización del tint para ProgressBar de RatingBar -->
        <item name="android:progressTint">@color/colorProgressBarNight</item>

        <!-- Otros atributos del tema -->
        <!-- Por ejemplo, personalización de la barra de estado -->
        <item name="android:statusBarColor">@color/colorPrimaryDarkNight</item>

        <!-- Personalización de los widgets, si es necesario -->
        <!-- Por ejemplo, cambiar el color de la barra de desplazamiento -->
        <item name="android:scrollbarThumbVertical">@color/colorPrimaryDarkNight</item>

    </style>
</resources>